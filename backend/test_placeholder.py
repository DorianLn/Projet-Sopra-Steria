# A simple placeholder test to ensure pytest runs correctly.

def test_placeholder():
    """
    This test is a placeholder to make sure the CI pipeline passes
    when no other tests are present. It can be removed once
    real tests are added.
    """
    assert 1 + 1 == 2
